services:
  - type: worker
    name: mdalpha-bot
    runtime: python
    buildCommand: pip install -r requirements-bot.txt && playwright install chromium
    startCommand: python run_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
